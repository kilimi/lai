# sam3 imports pkg_resources; setuptools 82+ removed it — keep <82 in this image.
setuptools>=69.0.0,<82
flask
werkzeug<3.0  # Pin to 2.x to avoid trusted host issues in Docker
pillow
numpy<2
opencv-python-headless
gunicorn
requests
huggingface_hub
einops
scikit-learn
scipy
decord
pycocotools
# INSID3 / DINOv3 (torch.hub) — not all pulled in by SAM stack; see INSID3 requirements.txt
torchmetrics>=0.11
termcolor>=1.1.0
tqdm>=4.60
matplotlib>=3.7
# SAM-2 and sam3 are installed from requirements-git.txt with --no-build-isolation
# (see Dockerfile) to avoid pip build-isolation failing to build SAM-2.
